# Case: DAN DAN 心魅かれてく

Song reference: "渐渐被你吸引".

This case is intentionally limited to the short title phrase. It validates that the existing local lyric reading override produces the expected romaji.

Expected behavior:

- `draft-romaji-corrections` creates zero corrections.
- `apply-review-decisions` applies zero decisions.
- Exported Markdown keeps `Romaji：DAN DAN kokoro hikareteku`.
